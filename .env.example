# sanitiser-dashboard — Example Client Configuration
# =====================================================
# Copy this to .env for local tool usage.
# The .env file is gitignored — do not commit it.

# sanitiser-dashboard endpoint (deployed on Docker host)
DASHBOARD_URL=http://your-docker-host:8107

# Back-link URL to AI Control Hub (shown in dashboard nav bar)
# Leave empty or omit to disable the back-link.
CONTROL_HUB_URL=
